name: pocket-js-server

services:
  pg:
    image: bitnami/postgresql:13.16.0
    ports:
      -'5432:5432'
    environment:
      - POSTEGRES_USER=docker
      - POSTEGRES_PASSWORD=docker
      - POSTEGRES_DB=inorbit

